interface:
  display_name: "Not Long; Just Read"
  short_description: "Local daily reading brief from private files"
  default_prompt: "Use $not-long-just-read to scan my subscriptions for direct new articles, deduplicate them against the local ledger, and generate today's daily feed."
