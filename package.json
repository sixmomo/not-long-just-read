{
  "name": "not-long-just-read",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first Not Long; Just Read daily briefing kit with an agent skill and local Web UI.",
  "scripts": {
    "validate": "python scripts/nljr.py validate",
    "generate": "python scripts/nljr.py generate",
    "dev": "python scripts/nljr.py serve"
  }
}
